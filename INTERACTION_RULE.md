# 个人 Skill 发现规则：不要求用户记住名称

这是一条轻量发现规则，不是总控 Agent，也不替代任何专业 Skill。每次应查看当前可用的个人 Skills，而不是永远停留在一张固定清单。

当用户从手机端或电脑端直接发送零碎念头、图片、截图、文章、PDF、观点或任务记录时：

1. 先判断输入最可能对应哪一个已安装的个人 Skill。
2. 如果用户意图清楚，并已明确要求一种处理方式，可直接调用最匹配的 Skill，并用一句话告诉用户。
3. 如果意图不清楚或存在两个合理方向，不要擅自执行；只问一个选择问题。
4. 问题使用任务名称，不要求用户记住英文 Skill 名。
5. 用户确认后，再调用对应 Skill；一次默认只调用一个。
6. 若没有已安装 Skill 适合，正常回答，不强行套用。

推荐询问格式：

> 我判断这段内容最适合先做“结构化思考”。要现在这样处理吗？

存在两个合理方向时：

> 这张图可以先“提炼知识入库”，也可以直接“发展成内容选题”。你想先做哪一个？

当前主要用户可见名称：

- 结构化思考 → `$structure-my-thinking-v1`
- 提炼知识入库 → `$distill-knowledge-v1`
- 形成并校准观点 → `$calibrate-my-view-v1`
- 发展内容与资产 → `$assetize-content-idea-v1`
- 发现重复工作流 → `$discover-my-workflows-v1`
- 内容雷达 → `$content-strategy-radar`

该规则只负责“发现、建议和确认”。意图明确时可以直接调用一个 Skill；意图含糊时必须先问。它不自动串联多个 Skill，也不构成总控 Agent。
